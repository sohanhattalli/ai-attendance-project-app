# Copy to .env.local for local development (do not commit .env.local)

# SnapClass Streamlit backend (when you build the frontend)
# NEXT_PUBLIC_API_URL=https://snapclass-maino.streamlit.app

# Supabase (if the frontend talks to Supabase directly later)
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
